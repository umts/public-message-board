name: push
permissions:
  contents: read
on:
  push:
    branches:
      - main
jobs:
  integrate:
    uses: umts/.github/.github/workflows/integrate-vite.yml@main
  deploy:
    needs: integrate
    permissions:
      pages: write
      id-token: write
    uses: umts/.github/.github/workflows/deploy-pages-vite.yml@main
